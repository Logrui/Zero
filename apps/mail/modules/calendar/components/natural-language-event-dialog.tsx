"use client";

// Placeholder to satisfy imports until we wire AI features (user declined to port NL dialog now).
export function NaturalLanguageEventDialog(_props: {
  open: boolean;
  onOpenChange: (o: boolean) => void;
  userId?: string;
  onEventCreated?: () => void;
}) {
  return null;
}
